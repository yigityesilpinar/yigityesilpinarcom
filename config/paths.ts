import path from 'path'

export const PROJECT_ROOT_DIR = path.resolve(__dirname, '../')

export const SRC_PATH = path.join(PROJECT_ROOT_DIR, 'src')
export const OUTPUT_DIR = path.join(PROJECT_ROOT_DIR, 'build')
